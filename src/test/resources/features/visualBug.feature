@Selenium
Feature: Testing Visual Bug in Boozang
  Scenario: Visual Bugs Testing
    Given open url https://thelab.boozang.com/visualBugs
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button
    When assert the name and image is the same
    Then click the NEXT IMAGE button